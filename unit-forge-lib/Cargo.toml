[package]
name = "unit-forge-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
logos = "0.15.0"
thiserror = "2.0.12"
indexmap = {version = "2.9", features = ["serde"]}

[dev-dependencies]
toml = "0.7"
